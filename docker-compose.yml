version: '3.8'

services:
  feishu-claude-bot:
    build: .
    container_name: feishu-claude-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
